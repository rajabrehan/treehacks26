# OpenLobby (TreeHacks 2026)

This repo contains:

- `/Users/akhildeo/treehacks26/docs/plan.md`: full architecture + build spec
- `/Users/akhildeo/treehacks26/openlobby-web`: Next.js noir-newsroom landing page + demo mode (seeded headlines/cases/images)

## Run The Web Demo

```bash
cd openlobby-web
npm install
npm run dev
```

Demo mode is enabled by default via `/Users/akhildeo/treehacks26/openlobby-web/.env.local.example`.
